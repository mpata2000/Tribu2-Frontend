export interface ITicketsDefaultState {
    loading: boolean,
    tickets: any[],
}

export interface IProductsDefaultState {
    loading: boolean,
    products: any[],
}

export interface ITareasDefaultState {
    loading: boolean,
    tareas: any[],
}

export interface IResourcesDefaultState {
    loading: boolean,
    resources: any[],
}